# 개인정보 검출 스캐너 실행/빌드에 필요한 라이브러리
# ── GUI ──────────────────────────────────────────────
customtkinter>=5.2.0

# ── 파일 형식 파서 (선택: 없으면 해당 형식만 건너뜀) ──
python-docx>=1.1.0      # .docx
openpyxl>=3.1.0         # .xlsx, .xlsm
pdfplumber>=0.11.0      # .pdf
olefile>=0.47           # .hwp

# ── 삭제 시 휴지통 이동(복구 가능) 지원 (선택) ──
Send2Trash>=1.8.0
